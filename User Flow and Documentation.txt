================================================================================
  TC UNDERGROUND — USER FLOW & DOCUMENTATION
  Domain Expansion Interface v1.0
  tuliocareli.com/underground
================================================================================

  "Este é o meu laboratório digital.
   Um ambiente seguro para experimentos de interface,
   integrações complexas de API e brincadeiras visuais
   que seriam arriscadas demais para o portfólio principal."

                                                         — Túlio Careli


================================================================================
  1. SITEMAP
================================================================================

  [PÁGINA PRINCIPAL — index.html]
  │
  ├── INTRO: Domain Expansion (tela cheia animada, auto-fecha em 3.5s)
  │
  ├── HEADER
  │   ├── Logo TC Underground
  │   ├── Terminal typewriter (loop com frases do sistema)
  │   ├── Botão >> VOLTAR AO PORTFÓLIO → tuliocareli.com
  │   └── Botão INICIAR → abre o TC Underground OS
  │
  ├── SIDEBAR ESQUERDA
  │   ├── TulioAmp (player de música simulado)
  │   │   ├── Equalizer interativo (faders arrastáveis)
  │   │   └── Playlist com 11 faixas
  │   ├── Banner YouTube (link → @Fonzarelli23)
  │   └── Spotify // Now Playing (widget mock animado)
  │
  ├── CONTEÚDO CENTRAL
  │   ├── Message Log (texto descritivo do lab)
  │   ├── Painel: O QUE ESTOU ASSISTINDO (grid de imagens)
  │   ├── Painel: O QUE ESTOU LENDO (grid de capas de livros)
  │   └── Painel: O QUE ESTOU OUVINDO (grid de artistas)
  │
  ├── SIDEBAR DIREITA
  │   ├── THPS Score Widget (placar animado estilo Tony Hawk)
  │   ├── Links Sociais:
  │   │   ├── Steam → steamcommunity.com/id/tuliocareli
  │   │   ├── Letterboxd → letterboxd.com/tuliocareli
  │   │   └── Enjoei → enjoei.com.br/@tulio23
  │   ├── GIF Pokemon // Now Roaming
  │   └── GIF Gato // Companion
  │
  ├── FOOTER
  │   ├── Logo + links (Steam, Letterboxd, YouTube)
  │   ├── Status do sistema (online/Spotify)
  │   ├── Relógio em tempo real (HH:MM:SS)
  │   └── Botão INICIAR (alternativo) → abre o TC Underground OS
  │
  └── CLIPPY HELPER (📎 — aparece após 10s sem o OS aberto)


================================================================================
  2. USER FLOW — TC UNDERGROUND OS (Desktop Simulado)
================================================================================

  ENTRADA NO OS:
  ─────────────
  Usuário clica em [INICIAR] (header ou footer)
       │
       ▼
  SFX: startup.mp3 toca
       │
       ▼
  Desktop XP-style é montado (lazy mount — só renderiza 1 vez)
       │
       ▼
  Grade de ícones exibida + Taskbar + Relógio do sistema
       │
       ▼
  Clippy OS (📎) aparece com dica contextual


  ÍCONES DO DESKTOP (clique duplo para abrir janela):
  ────────────────────────────────────────────────────
  🖥️  Meu Computador
       ├── Disco Local (C:)    → não faz nada
       ├── CD-ROM (D:)         → abre janela de músicas (lista de faixas)
       ├── Impressora HP       → não faz nada
       └── Monitor LG          → não faz nada

  🌐  TC Explorer (Internet Explorer mockup com abas)
       ├── Tony Hawk Underground     → Wayback Machine (site original 2003)
       ├── YouTube (2007)            → Mockup interativo do YT antigo
       │    └── Vídeos do canal @Fonzarelli23 via YouTube Iframe API
       ├── Orkut                     → Mockup interativo do Orkut
       │    ├── Tela de Login
       │    ├── Perfil do Túlio (scraps persistidos em localStorage)
       │    └── App: BuddyPoke (Flash via Ruffle)
       ├── Jogos Online              → Mockup do jogosonline.com.br
       │    └── Jogos Flash jogáveis (Ruffle):
       │         ├── TG Motocross 2
       │         ├── BMX Stunts
       │         └── Create a Ride
       ├── NFS Underground 2         → Wayback Machine (site original 2004)
       └── Cartoon Network           → Wayback Machine (site original 2004)

  🎧  Tulioamp
       └── Player WinAmp-style com playlist, EQ e visualizador (modo desktop)

  🎨  Tulio Paint
       └── Tela de pintura interativa (tipo MS Paint)

  🌎  Tulio Earth
       ├── Globo 3D Interativo (CesiumJS)
       ├── Sistema de relevo e atmosfera dinâmica
       └── Street View Integrado (MapillaryJS)
           └── Busca automática baseada no centro da visão do globo

  💿  Tulio Burning ROM
       └── Simulação de gravação de CD com progresso

  💬  Tulio Messenger
       └── Chat com o Túlio AI (integração Gemini) — MSN-style

  📝  TulioPad
       └── Bloco de notas com texto editável

  🎬  Tulio Media Player
       └── Player de mídia (Windows Media Player style)

  🖼️  Imagens
       └── Visualizador de imagens (type: grid/galeria)

  📟  Calculadora
       └── Calculadora funcional

  💣  Campo Minado
       └── Minesweeper jogável

  ☎️  TulioNet 56K
       └── Simulação de conexão discada com sons e progresso

  🍋  TulioShare PRO
       └── LimeWire/eMule mockup

  🚀  Internet_Acelerator.exe
       └── "Vírus" falso — congela a janela (efeito ghost trail ao arrastar)

  doom.exe
       └── Doom clássico operando via emulação x86 (js-dos)
           └── Suporte a teclado e mouse (lock pointers)

  ⬛  terminal.exe
       └── Terminal hacker animado

  📄  README.txt
       └── Abre texto de apresentação do sistema (Notepad-style)

  📄  GTA_Cheats.txt
       └── Lista de cheats do GTA (Notepad-style)

  💽  Limpar_Cache_Rapido.exe
       └── Tela azul da morte (BSOD) com reinicialização falsa

  📁  Meus Projetos
       └── Pasta com links para projetos do portfólio

  🗑️  Lixeira
       └── Lixeira vazia (efeito de esvaziamento ao clicar)

  📂  mysterious_folder (SECRETO)
       └── Aparece após N interações — conteúdo surpresa


  DETALHAMENTO DE FLUXOS COMPLEXOS:
  ─────────────────────────────────

  🌎 FLUXO TULIO EARTH (3D & Street View):
  1. Usuário abre Tulio Earth.
  2. CesiumJS inicializa o globo 3D usando o token Ion servido pela API.
  3. Navegação: Usuário pode voar pelo mundo, ativar relevo 3D ou atmosfera.
  4. Street View: Ao clicar em [Street View Aqui!]:
     a. O sistema dispara um raio (Raycasting) do centro da tela para o globo.
     b. Captura a coordenada exata de Latitude/Longitude onde o usuário está "olhando".
     c. Consulta a API Mapillary V4 em busca de imagens de rua num raio de 50m.
     d. Se encontrada, o MapillaryJS renderiza um overlay 360° dentro da própria janela.
     e. Usuário pode navegar pela rua ou clicar em "Sair" para voltar ao globo.

  🚀 FLUXO DOOM.EXE (Emulação em Browser):
  1. Usuário clica duas vezes no doom.exe.
  2. O sistema carrega dinamicamente a biblioteca js-dos.js (Kernel do DOSBox).
  3. Monta o bundle "doom.jsdos" no sistema de arquivos virtual do WASM.
  4. Inicializa o áudio via Web Audio API e captura os inputs de teclado/mouse.
  5. Controles: Setas para mover, CTRL para atirar, Barra de Espaço para interagir.
  6. Ao fechar a janela, o callback onClose força o encerramento da engine para
     liberar CPU e memória RAM.


  COMPORTAMENTO DAS JANELAS:
  ──────────────────────────
  - Arrastáveis (mouse + touch)
  - Minimizar / Restaurar via Taskbar
  - Maximizar / Restaurar
  - Fechar (X)
  - Múltiplas janelas abertas simultaneamente
  - Z-index dinâmico (janela clicada vai para frente)
  - SFX de abertura, erro e fechamento


  SAÍDA DO OS:
  ─────────────
  Usuário clica em [Iniciar] → [Desligar] na taskbar do OS
       │
       ▼
  SFX: shutdown.mp3 toca
       │
       ▼
  Tela de shutdown com animação
       │
       ▼
  Desktop é desmontado — retorna à página principal


================================================================================
  3. TECNOLOGIAS UTILIZADAS
================================================================================

  FRONTEND
  ────────
  Linguagem:   HTML5 + CSS3 + JavaScript (Vanilla ES6+)
  Arquivo CSS: style.css, desktop.css, apps.css, thps.css
  Arquivos JS: desktop.js (OS completo), script.js (página principal)

  Principais técnicas:
  - Canvas API (animações de intro: starfield + matrix + header rain)
  - Drag & Drop manual (janelas do OS, sem libs externas)
  - requestAnimationFrame (animadores de canvas e visualizador)
  - localStorage (scraps do Orkut persistidos no browser)
  - Z-index dinâmico (gerenciamento de foco de janelas)
  - Micro-interações e SFX com Web Audio API (arquivos .mp3 em /sons)
  - CSS Custom Properties (sistema de design, tema neon/retrô)
  - CSS Animations (glitch loop, domain rings, THPS score, etc.)
  - Raycasting de centro de câmera (CesiumJS → MapillaryJS)


  BACKEND / DEPLOY
  ────────────────
  Plataforma:  Vercel (Serverless Functions)
  Runtime:     Node.js 24.x
  Entry:       api/chat.js (Vercel Serverless Function)

  CORS configurado para:
  - https://tuliocareli.com
  - http://localhost:3000
  - http://127.0.0.1:5500
  - *.dominiodotulio.vercel.app (regex preview deploys)


  ANALYTICS
  ─────────
  Ferramenta:  Vercel Web Analytics
  Método:      Snippet vanilla JS (/_vercel/insights/script.js)
  Dados:       Pageviews, referral, Web Vitals — sem cookies.


================================================================================
  4. APIs E INTEGRAÇÕES
================================================================================

  ┌─────────────────────────────────────────────────────────────────────────┐
  │  GOOGLE GEMINI API                                                       │
  │  Propósito:   Alimenta o "Túlio AI" no app Tulio Messenger (MSN-style)  │
  │  Pacote:      @google/generative-ai ^0.21.0                             │
  │  Modelo:      gemini-flash-latest                                        │
  │  Endpoint:    POST /api/chat                                             │
  │  Auth:        GEMINI_API_KEY (env var na Vercel, nunca exposta)          │
  │  System Prompt: personagem "Túlio", designer de BH, digitando como      │
  │                 se fosse MSN 2005, com travas contra jailbreak           │
  │  Limitações:  Respostas curtas (máx 3 frases), assuntos restritos       │
  └─────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────────┐
  │  YOUTUBE IFRAME API                                                       │
  │  Propósito:   Reprodução de vídeos do canal @Fonzarelli23 dentro do      │
  │               TC Explorer (aba "YouTube 2007")                           │
  │  Carregamento: Lazy — script injetado no DOM só quando a aba é aberta    │
  │  SDK:         https://www.youtube.com/iframe_api                         │
  │  Auth:        Pública (sem API key — uso apenas de iframe embed)         │
  └─────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────────┐
  │  WAYBACK MACHINE / INTERNET ARCHIVE                                       │
  │  Propósito:   Carregar versões históricas de sites dentro do TC Explorer  │
  │  Sites:       Tony Hawk Underground (2003), NFS Underground 2 (2004),    │
  │               Cartoon Network BR (2004)                                  │
  │  Método:      iframe apontando para URLs do web.archive.org              │
  │  Auth:        Pública (sem autenticação)                                 │
  └─────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────────┐
  │  RUFFLE (Flash Player Emulator)                                           │
  │  Propósito:   Emular e rodar arquivos .SWF (Flash) no browser moderno   │
  │  Apps usados: BuddyPoke (Orkut), TG Motocross 2, BMX Stunts,            │
  │               Create a Ride, Doom (versão flash)                        │
  │  Carregamento: https://unpkg.com/@ruffle-rs/ruffle                      │
  │  Auth:        CDN pública, sem autenticação                              │
  │  Limitação:   Texturas ActionScript 3 sem servidor original → alguns     │
  │               SWFs carregam parcialmente ("modo fantasma")               │
  └─────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────────┐
  │  SPOTIFY WIDGET (MOCK)                                                    │
  │  Propósito:   Exibir "Now Playing" na sidebar                            │
  │  Método:      Rotação automática de faixas hardcoded (sem API real)      │
  │  Nota:        Widget de aparência mas sem integração OAuth com Spotify.  │
  │               Futuras versões poderiam usar Spotify Web API + token.     │
  └─────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────────┐
  │  LOCALSTORAGE                                                             │
  │  Propósito:   Persistir "scraps" (recados) do Orkut simulado            │
  │  Chave:       "orkut_scraps"                                             │
  │  Formato:     JSON Array → [{ text: string, date: string }]             │
  │  Escopo:      Browser do usuário — limpar cache apaga os scraps          │
  └─────────────────────────────────────────────────────────────────────────┘
  
  ┌─────────────────────────────────────────────────────────────────────────┐
  │  CESIUM ION API (CESIUMJS)                                              │
  │  Propósito:   Motor do Tulio Earth (Mapa 3D, Terreno, Imagens)          │
  │  Token:       Servido via /api/cesium (env var CESIUM_ION_TOKEN)        │
  │  Recursos:    Bing Maps Aerial, Cesium World Terrain, Sun Lighting       │
  └─────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────────┐
  │  MAPILLARY V4 GRAPH API                                                 │
  │  Propósito:   Street View de código aberto integrado ao Earth           │
  │  Token:       Servido via /api/mapillary (env var MAPILLARY_CLIENT_TOKEN) │
  │  Lógica:      Busca por proximidade (lat/lng/radius=50) baseada no      │
  │               ponto central de colisão da câmera do Cesium.             │
  └─────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────────┐
  │  JS-DOS (Lib: Emulator Core)                                            │
  │  Propósito:   Engine para rodar doom.exe (Doom 1 Shareware)             │
  │  Método:      Execução de bundle .jsdos via WebAssembly em canvas       │
  │  Kernel:      js-dos.js / js-dos.wasm local em /libs/js-dos             │
  │  Controles:   Setas=Mover, CTRL=Tirar, Space=Abrir Portas               │
  │  Memória:     Callback onClose() mata a instância ci.exit() para        │
  │               evitar loops de áudio e CPU após fechar a janela.         │
  └─────────────────────────────────────────────────────────────────────────┘


================================================================================
  5. ESTRUTURA DE ARQUIVOS (RESUMO)
================================================================================

  /
  ├── index.html               — Entrada principal, layout 3 colunas
  ├── style.css                — Design system base, variáveis CSS, layout
  ├── desktop.css              — Estilos do OS (janelas, taskbar, ícones)
  ├── apps.css                 — Estilos internos dos apps (TC Explorer, etc.)
  ├── thps.css                 — Score widget estilo Tony Hawk
  ├── script.js                — Animações e widgets da página principal
  ├── desktop.js               — Engine completa do TC Underground OS
  ├── package.json             — Node 24.x + @google/generative-ai
  ├── api/
  │   └── chat.js              — Serverless Function: chat com Túlio AI
  ├── sons/                    — SFX do OS (.mp3)
  ├── imagens/                 — Assets visuais gerais
  ├── videosjogos/             — Vídeos dos jogos (.mp4)
  ├── jogoflash/               — Arquivos SWF (Flash)
  ├── jogosicon/               — Ícones dos jogos
  ├── o que estou lendo/       — Capas dos livros
  ├── o que estou ouvindo/     — Capas dos artistas
  └── imagens do que estou vendo no momento/  — Pôsteres de séries/animes


================================================================================
  FIM DO DOCUMENTO
  TC UNDERGROUND // Domain Expansion Interface v1.0
================================================================================
